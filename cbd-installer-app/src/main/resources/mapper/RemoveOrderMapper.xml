<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cbd.installerapp.dao.waitingtask.RemoveOrderDao">
    <insert id="removeDev" parameterType="com.cbd.cbdcommoninterface.request.installerapp.waitingtask.RemoveQuery">
        INSERT INTO deviceremovelog VALUES(NULL,#{installerId},#{devId},#{removeReason},#{removeTime},#{removeResult});
    </insert>

</mapper>